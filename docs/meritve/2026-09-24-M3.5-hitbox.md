# M3.5 — višina jahača po sestopu, zagon v svetu (24. 9. 2026)

Zagona `r1-run.ps1` po commitu `2803076` (10:07 in 10:09 po lokalnem času):
`audit/m22-r1.log` (način 0) in `audit/m22-r1-k1.log` (`-Krmiljenje 1`). Oba `R1-SUM mount=8 progaM=8 progaS=8`.

| | pred mountom | po mountu | po sestopu |
|---|---|---|---|
| način 0 (original) | 1,900 | 1,463 (× 0,77) | **1,463** — ostane skrčena |
| način 1 (popravek) | 1,900 | 1,463 (× 0,77) | **1,900** — vrne se |

Vseh osem jahačev ima isto višino (min = max v obeh vrsticah `R1-HB`).

## Ugotovitve

- **E9 zeleno.** Način 0 ponovi napako originala (napoved iz `02-ZAHTEVE.md` §R1, popravljena v seji 56:
  mount je pravilen že v originalu, napačen je samo sestop). Način 1 višino po sestopu vrne.
- Faza A je v obeh zagonih enaka kot v M3.4 (način 0: M 1,19 bloka; način 1: M 22,57), torej popravek
  sestopa meritve faz A–C ne spremeni — sestop je po koncu faze C.
- Odjemalčeva stran (paket `SPacketSetPassengers`) na dedicated serverju ni preverjena.
