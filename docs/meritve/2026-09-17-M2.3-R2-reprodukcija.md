# 2026-09-17 — M2.3: reprodukcija R2 (leteči NPC in ovira)

**Zagon:** 17. 9. 2026, 12:15–12:17, `.\r2-run.ps1` na uporabnikovem računalniku.
**Scenarij:** [`../scenariji/M2.3-R2.md`](../scenariji/M2.3-R2.md).
**Log:** `audit/m23-r2.log` · **poročilo skripte:** `audit/m23-r2-2026-09-17-1217.md`.
**Izid meril:** L1–L3 in L5–L8 zelena, **L4a in L4b padla** — in prav ta padec je glavna
ugotovitev te meritve.

---

## Pogoji

| | |
|---|---|
| Trajanje | 1531 server tickov (≈ 77 s), tri faze po 440 tickov |
| NPC-jev | 21 (3 × 6 na progah + 3 cilji) + krmilnik |
| Prisilno naloženih chunkov | 59 → 65, obroč 2, zavrnjenih 0 (pogoj M2.1d izpolnjen) |
| Igralcev | 0 |
| `npc.per.tick` | p50 22, max 22 |
| `npc.update` | 33 664 klicev / 1531 tickov = **21,99 na tick** |
| `npc.update.window` | 3,573 ms/tick, povpr. 162,5 µs na NPC (zgornja meja, ne točna poraba) |
| `server.tick.ns` | p50 0,836 ms · p95 8,913 ms · p99 102,760 ms · max 183,113 ms |

MSPT p95 je 17,8 % proračuna 50 ms. Visoka p99 in max sta pri vsakem zagonu posamična
ticka ob autosave (znana omejitev, M2.5 jo mora izločiti).

---

## Izid po fazah in progah (konec faze)

| Faza | Proga | navig | cele | čez oviro | prevoženo povp./max | y povp./max | do cilja min | razpon |
|---|---|---|---|---|---|---|---|---|
| A | F leteči + zid | 0/6 | **0/6** | 0/6 | 7,95 / 8,82 | 4,17 / 5,00 | 9,30 | 6,65 → 1,26 |
| A | W kopenski + zid | 0/6 | 0/6 | 0/6 | 6,93 / 8,25 | 4,00 / 4,00 | 9,31 | 8,51 → 2,53 |
| A | P leteči, prosto | 0/6 | 6/6 | **6/6** | **16,27 / 17,35** | 4,83 / 5,00 | **1,00** | 2,21 |
| B | F | 6/6 | 0/6 | 0/6 | **0,00 / 0,00** | 4,00 | 16,01 | 10,00 |
| B | W | 4/6 | 0/6 | 0/6 | 6,83 / 7,89 | 4,00 | 9,31 | 1,93 |
| B | P | 6/6 | **6/6** | 0/6 | **0,00 / 0,00** | 4,00 | 16,01 | 10,00 |
| C | F | 6/6 | 0/6 | 0/6 | **0,00 / 0,00** | 4,00 | 16,01 | 10,00 |
| C | W | 0/6 | 0/6 | 0/6 | 6,98 / 7,58 | 4,00 | 9,30 | 2,90 |
| C | P | 6/6 | **6/6** | 0/6 | **0,00 / 0,00** | 4,00 | 16,01 | 10,00 |

Razdalja start → cilj je 16 blokov, zid stoji na 8 blokih poti in je visok 4 bloke
(y 4–7). `razpon` 10,00 je natanko razmik šestih NPC-jev po 2 bloka, torej postavitev
takoj po resetu.

---

## Faza A — edina veljavna faza tega zagona

Faza A teče od svežega spawna in ni odvisna od ničesar, kar je scenarij naredil prej. Njeni
trije nizi so primerljivi med sabo in odgovarjajo natanko na vprašanje iz R2.

**Proga P (leteči, brez ovire) — letenje deluje.** 16 blokov prevozi v ≈ 80 tickih (4 s),
`cele=6/6` ves čas, `cezOviro=6/6`, na koncu je najbližji NPC 1,00 bloka od cilja. Ko
pridejo, se ustavijo in **obvisijo** (`zastoj` 6/6, y ostane 4,83, ne padejo) — točno tako,
kot napoveduje `EntityNPCFlying.travel()`, ki pri `canFly()` gravitacije ne doda.

**Proga F (leteči, zid) — ne pride čez.** Ustavi se po 7,95 bloka, torej **pri zidu**
(zid je 8 blokov od starta). `cezOviro` je 0/6 v vseh 22 vzorcih. `y` doseže največ 5,00,
čeprav je vrh zidu pri 7: skupina se dvigne za en blok in tam obstane. `razpon` pade s 6,65
na 1,26 — **NPC-ji se pred oviro zgostijo**, enako kot pri R1.

**Proga W (kopenski, zid) — ne pride čez, kar potrjuje, da je zid ovira.** 6,93 bloka in
`cezOviro=0/6`. Merilo L5 je zeleno, torej je razlika F proti P veljavna.

**Ključna številka je `cele=0/6` na progi F, in sicer že v prvem vzorcu.** Leteči NPC pred
zidom ne obstane zato, ker bi vodenje odpovedalo, ampak zato, ker **iskanje poti sploh ne
vrne poti čez zid**. Pot čez zid je dolga ≈ 24 blokov, torej pod `NpcNavRange` 32 — domet
ni vzrok (to je bila past Q11 in scenarij jo je namenoma izključil). Ostane omejitev 200
vozlišč v `PathFinder.findPath:65`, ki je v 3D bistveno manj napredka kot v 2D. Dokaz tega
koraka je odprt in gre v M2.1b/M4 (instrumentacija `PathNavigate`).

**Za R2 to pomeni:** leteči NPC se pri oviri obnaša **enako kot kopenski** — ustavi se pred
njo, čeprav bi jo moral preprosto preleteti. Razlika med progama F in W v fazi A je
0,4 bloka prevožene poti in nič pri prehodu ovire. Letenje samo po sebi deluje (proga P),
torej ne gre za splošno okvaro letenja, ampak za **navigacijo, ki višine ne uporabi**.

---

## Fazi B in C — neveljavni: dvanajst letečih NPC-jev zmrzne

Po koncu faze A skripta NPC-je vrne na start z `setPosition`. Od tega trenutka naprej se
**noben od dvanajstih letečih NPC-jev (progi F in P) ne premakne niti za 0,01 bloka** — v
900 tickih, v dveh fazah, pri dveh različnih načinih vodenja (`navigateTo` osvežen in
`setAttackTarget`). Vsi ostanejo točno na y = 4,00, `razpon` je točno 10,00, `doCiljaMin`
točno 16,01.

Hkrati velja:

- `navig=6/6` in **`cele=6/6` na progi P**: navigator ima celo pot do cilja in jo obdrži.
- Kopenska proga W se v obeh fazah **premika normalno** (6,83 oziroma 7,15 bloka), torej
  reset z `setPosition` sam po sebi ni pokvarjen.
- Progi F in P sta zmrznili obe, čeprav je ena za zidom in druga na prazni ravnini. Ovira
  torej ni vzrok.

To je **drugi, samostojen pojav**, ne del R2: NPC z veljavno, celo potjo, ki se ne premakne.
Dokler ni pojasnjen, fazi B in C ne povesta o oviri ničesar, zato L4a in L4b pravilno padeta.

**Kaj je že izključeno.** Števec `npc.update` kaže 21,99 klicev na tick čez vseh 1531
tickov, torej je svet vse NPC-je posodabljal ves čas, tudi v fazah B in C. Razlaga „svet
entitet ne posodablja" je s tem malo verjetna, a je števec agregaten in ne po entiteti.

**Kaj ostane.** Tri razlage, ki se med seboj izključujejo:

1. entitete se ne posodabljajo (`World.updateEntityWithOptionalForce` jih preskoči),
2. posodabljajo se, vodenje pa jim ne doda gibanja — `FlyingMoveHelper:50` postavi `WAIT`
   in tam ostane,
3. gibanje je, a ga `move()` poje ob trku.

Prvi zagon med njimi ni znal izbirati, ker sta manjkali dve številki. Zato sta v vzorec
dodana **`dStarost`** (`getAge()` = `ticksExisted`, loči 1 od 2 in 3) in **`gib`**
(`motionX/Y/Z`, loči 2 od 3), ter merili **L9** (entitete se tikajo) in **L10** (merilnik
gibanja dela). `r2-run.ps1` iz njiju sam sestavi razdelek „Proge, ki se niso premaknile".
Razlaga pride iz naslednjega zagona, ne iz ugibanja.

---

## Česa ta meritev ne pove

- **Ne pove, zakaj** iskanje poti na progi F ne najde poti čez zid. Omejitev 200 vozlišč je
  hipoteza, ne izmerjeno dejstvo; µs na iskanje in iskanj na tick sta veličini 5 in 6 iz
  M2.7 in zahtevata klicna mesta v `PathNavigate` (M2.1b).
- **Ne pove ničesar o načinu C** (dragon/momentum) ali nagibu — ne obstajata.
- **Ne pove, ali leteči NPC kot vozilo z igralcem deluje** (Q6 je še odprt).
- Meritev je tekla na dedicated serverju brez igralca, z prisilno naloženimi chunki. Z
  igralcem v svetu so številke lahko drugačne.
- Zmrznitev iz faz B in C **ni bila preverjena z `javap`** in nima svoje reprodukcije; do
  naslednjega zagona je hipoteza, ne bug.

---

## Naslednji korak

1. Ponoviti `.\r2-run.ps1` z dodanima `dStarost` in `gib` (merili L9 in L10).
2. Izid vpisati sem kot razdelek „Ponovitev" in zmrznitev bodisi razložiti bodisi zavreči
   kot napako scenarija.
3. Šele nato M2.4 ali M2.7.
